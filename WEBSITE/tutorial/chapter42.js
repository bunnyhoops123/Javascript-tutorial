//SHuffle an array

//Not complete to be revisted

let cards = [1,2,3,4,5,6,7,8,9];
shuffle(cards);

function shuffle(array){
    let currentIndex = array.length;

    while(currentIndex != 0){
        let random = Math.floor(M)
    }


}
