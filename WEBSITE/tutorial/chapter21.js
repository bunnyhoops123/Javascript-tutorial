//Funtions = Define code once and re-use it multiple times

let userName = "Sumukh Varma";
let age = 21;

function wishBday(name, num){
    console.log(name, num);
    this.name;
}

wishBday(userName, age);